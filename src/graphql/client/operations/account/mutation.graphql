mutation UpdateProfile($input: UpdateProfileInput!) {
    profileUpdate(input: $input) {
        errors {
            message
        }
        account {
            ...AccountPayloadFragment
        }
    }
}

mutation SendEmail {
    sendEmail
}
